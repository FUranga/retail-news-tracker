# Scratch space for /draft — personal drafts, not part of the published dataset
drafts/

# Local pointer CSV, regenerated on every scraper run — no need to version it
lse_latest.csv

__pycache__/
*.pyc
# Archivos locales de automatización — específicos de cada máquina
update_retail_wire.ps1
retail_wire_update.bat
setup_task_scheduler.ps1

# Salida de diagnose_sources.py — se regenera en cada corrida, no es dataset publicado
data/diagnose_sources_last_run.json
